package io.github.sandydunlop.cupra.common.util;

/*
 * How far into the tree of widgets, widget components, scrollable widgets
 * we go when looking for what the mouse pointer is hovered over. 
 */
public enum DepthLimit {
    COMPONENT,
    WIDGET,
    SCROLLABLE
}
